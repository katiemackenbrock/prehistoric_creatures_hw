<h3> It's dino time!</h3>
<h4> Who's coming to the dino party?</h4>

<ul>
    <% dinos.forEach(dino => { %>
    <li>
        <p><strong><%= dino.name %></strong> is a <%= dino.type %></p>
    </li>
    <% }) %>
</ul>